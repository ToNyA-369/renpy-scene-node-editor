# 階段性架構規格

這份文件整理目前已定案的遊戲架構。目標是讓內容創作者主要透過少量 JSON 與 Ren'Py `.rpy` 文件填充內容，而不需要處理底層流程實作。

此架構適合以 `Option` 作為唯一玩家輸入，並以文字、圖片、影片、音效、UI 等作為輸出的 SLG / stat-based VN 類遊戲。

---

## 1. 核心概念

### 1.1 Interface

遊戲介面由三種元素組成：

```text
Option           玩家輸入
Visual / Audio   視覺與聲音輸出
Text             文字輸出
```

其中 `Option` 是玩家唯一的輸入來源。玩家選擇後會產生一個 `Trigger`，再交由目前 Scene Node 的 Event Pool 選出對應 Event。

### 1.2 State

`State` 是全遊戲唯一的資料系統。

State 主要包含：

```text
Stats      數值資料，例如金錢、體力、好感度、時間
Memories   依記憶庫分類的標籤，例如已觸發事件、今日已行動、章節狀態
```

所有 Scene Node 共用同一個 State。Scene Node 不擁有自己的 State。

### 1.3 Scene Node

凡是有選項的互動單位都是 `Scene Node`。

每個 Scene Node 擁有自己的：

```text
Node.json        節點設定
Options.json     預設的資料化玩家選項
SCENEOPTION.rpy  進階自訂玩家選項
EVENTPOOL/       該節點可觸發的事件
CONTENT/         該節點會呼叫的 Ren'Py label
```

每個 Scene Node 都有自己的 Event Pool，但所有 Scene Node 共同讀寫全域 State。

### 1.4 Event

`Event` 是 Scene Node 中的反應單位。

一次玩家 Action 只會選中一個 Event。系統會依序透過 `Trigger`、`Conditions`、`Priority`、`Weight` 選出本次要執行的 Event。

Event 可以：

```text
檢查 Conditions
改變 State
新增、移除或清空記憶庫標籤
呼叫 Content label
決定節點結束行為
```

### 1.5 Content

`Content` 由創作者自行撰寫 Ren'Py label。

框架只負責決定要呼叫哪個 label，不負責管理 label 內部的演出細節。

Content 可以是 `null`。這代表該 Event 只執行 Effects 與節點流程，不呼叫任何 Ren'Py label。

Content label 原則上只負責演出，State 改變應優先寫在 Event 的 `Effects` 裡。

---

## 2. 創作者文件結構

目前定案的創作者側文件結構如下：

```text
DATA/
  SceneProject.json
  Stats.json
  Memories.json

SCENENODE/
  <node_path>/
    Node.json
    Options.json
    SCENEOPTION.rpy
    EVENTPOOL/
      <event_id>.json
    CONTENT/
      <label_name>.rpy

SCENESCREEN/
  <screen_id>.rpy
```

### 2.1 命名規則

資料夾名稱可以採用全大寫無空白：

```text
DATA
SCENENODE
SCENESCREEN
EVENTPOOL
CONTENT
```

節點可以用樹狀資料夾擺放，例如：

```text
SCENENODE/
  HOME/
    BEDROOM/
      Node.json
      Options.json
      SCENEOPTION.rpy
      EVENTPOOL/
      CONTENT/
```

但 `Node ID` 建議在全遊戲中保持唯一。

創作者可以使用中文顯示名稱。編輯器在建立 Node、Event、Stat、Memory Bank、Content 與 Screen 時，會另外產生穩定的 ASCII 技術 ID。顯示名稱可改，技術 ID 不會跟著改名，藉此保護 Ren'Py label、screen、JSON 引用與存檔穩定性。

Trigger、記憶標籤、備註與玩家看到的選項文字可以直接使用中文。

### 2.2 DATA/SceneProject.json

`SceneProject.json` 保存專案層級設定。空白專案初始化時會建立 ROOT Scene Node，並以穩定技術 ID 記錄：

```json
{
  "Version": 1,
  "Root Node": "root"
}
```

Runtime 呼叫 `scene_runtime_start()` 時會從這裡取得起始節點。既有的 `scene_runtime_start("node_id")` 仍受支援，顯式 ID 優先於專案設定。要刪除目前 Root 前，必須先指定另一個 Scene Node 為新的起始節點。

---

## 3. DATA/Stats.json

`Stats.json` 定義全遊戲有哪些數值型資料。

格式：

```json
{
  "Money": {
    "Name": "金錢",
    "Max": 99999999,
    "Min": 0,
    "Init": 0
  },
  "Energy": {
    "Name": "體力",
    "Max": 100,
    "Min": 0,
    "Init": 100
  }
}
```

欄位說明：

```text
Name   創作者可讀的顯示名稱
Max    最大值
Min    最小值
Init   初始值
```

建議框架在套用 Effects 後自動將 Stats 限制在 `Min` 與 `Max` 之間。

---

## 4. DATA/Memories.json

`Memories.json` 定義全遊戲可使用的記憶庫。每個記憶庫在 Runtime 存放一組不重複的字串標籤。

格式：

```json
{
  "memory": {
    "Name": "Memory"
  },
  "daily": {
    "Name": "每日記憶"
  }
}
```

`memory` 是系統保留的預設記憶庫，不能移除。`Once: true` 與舊版 Tag 資料都使用這個預設庫。其他記憶庫由創作者在編輯器「狀態」工作區建立。

記憶庫本身不再帶有 `permanent / daily / weekly` 等硬編碼生命週期。何時清空由遊戲流程明確決定，例如遊戲的換日程式可呼叫：

```renpy
$ scene_memory_clear("daily")
```

Runtime API：

```text
scene_memory_has(bank_id, tag_id)
scene_memory_add(bank_id, tag_id)
scene_memory_remove(bank_id, tag_id)
scene_memory_clear(bank_id)
```

舊版 Event 中的 `type: "tag"` 會在讀取時視為預設 `memory` 記憶庫；下次由編輯器儲存該筆資料時轉成新格式。舊存檔裡三組 Tag 也會合併到預設記憶庫一次。

---

## 5. SCENESCREEN

`SCENESCREEN/` 存放可重用的 Ren'Py Screen。

Scene Screen 是 UI 外殼，例如：

```text
文字選項畫面
房間探索畫面
手機 UI
地圖 UI
```

Scene Screen 不負責選 Event，也不直接改變 State。它只負責顯示畫面與承載互動 UI。

Scene Node 透過 `Node.json` 指定要使用哪個 Screen。

---

## 6. SCENENODE/Node.json

`Node.json` 定義單一 Scene Node 的基本設定。

格式：

```json
{
  "ID": "homeBedroom",
  "Name": "我的房間",
  "Background": "bedroomDay",
  "Screen": "basicScene",
  "Option Mode": "DATA",
  "Option Screen": "scene_option_renderer"
}
```

欄位說明：

```text
ID          節點 ID，建議全遊戲唯一
Name        創作者可讀的顯示名稱
Background 進入節點時使用的背景圖或背景資源名稱
Screen      使用的 Scene Screen 名稱
Option Mode `DATA` 使用 Options.json；`CUSTOM` 使用自訂 Screen
Option Screen DATA 固定為 scene_option_renderer；CUSTOM 時填 Screen 技術 ID
```

`Background` 與 `Screen` 的實際呈現方式由框架與對應 `.rpy` 實作決定。

---

## 7. SCENENODE/Options.json 與 SCENEOPTION.rpy

`Options.json` 是預設的玩家選項來源。編輯器可用表單建立三種 Element：

```text
TEXTBOX  垂直選項清單
PICTURE  圖片按鈕
HITBOX   場景互動區域
```

每個 Element 具有穩定 ID、中文名稱、Layout、顯示條件與可用條件。TEXTBOX 另外包含 Items、最多顯示數量、Item 高度與間距、Padding、Scrollbar、滑鼠滾輪與拖曳設定。Scrollbar 可設為 `AUTO`、`HIDDEN` 或 `ALWAYS`，超過最多顯示數量的 Item 仍可透過捲動查看。

編輯器預設顯示名稱、位置、尺寸、Trigger、文字與最多顯示數量等常用欄位；樣式、條件、音效、Scrollbar 細節與自訂 RPY 收在「進階選項」。畫布可直接拖曳與縮放 Element，TEXTBOX 的垂直縮放會吸附成整數列數。

概念格式：

```json
{
  "Version": 1,
  "Canvas": {
    "Width": 1920,
    "Height": 1080,
    "Preview Background": "gui/main_menu.png"
  },
  "Elements": [
    {
      "ID": "option_actions",
      "Name": "行動清單",
      "Type": "TEXTBOX",
      "Layout": { "X": 650, "Y": 360, "Width": 620, "Height": 274, "Z Order": 10 },
      "List": { "Max Visible Items": 3, "Scrollbar": "AUTO", "Mousewheel": true, "Draggable": true },
      "Items": [
        { "ID": "option_walk", "Name": "散步", "Text": "散步", "Trigger": "Action:散步" }
      ]
    }
  ]
}
```

若資料化 Renderer 無法表達特殊 UI，可把 `Option Mode` 改為 `CUSTOM`，並在同節點的 `SCENEOPTION.rpy` 使用 Ren'Py 原生 Screen。兩種模式共用同一個核心契約：

```text
選項只回傳 Trigger，不直接選 Event。
```

Trigger 建議格式：

```text
Action:<action_id>
```

概念範例：

```renpy
Return("Action:buyWater")
```

保留 Trigger：

```text
Auto   進入節點時自動檢查的事件
```

`Options.json` 與自訂 SCENEOPTION 都不應直接呼叫 Content label，也不應直接 GOTO 其他 Scene Node。節點流程應交由 Runner 統一處理。

---

## 8. SCENENODE/EVENTPOOL/<event_id>.json

每個 Event 以獨立 JSON 文件存放於 Scene Node 的 `EVENTPOOL/` 底下。

建議檔名與 Event ID 一致：

```text
EVENTPOOL/
  buyWater.json
  talkNormal.json
  autoMorning.json
```

### 8.1 Event 基本格式

```json
{
  "ID": "buyWater",
  "Name": "購買瓶裝水",
  "Trigger": "Action:buyWater",

  "Priority": 5,
  "Weight": 50,
  "Once": false,

  "Conditions": [
    { "type": "stat", "id": "Money", "op": ">=", "value": 50 }
  ],

  "Effects": [
    { "type": "stat", "id": "Money", "op": "-", "value": 50 },
    { "type": "memory", "bank": "memory", "op": "add", "id": "得到一瓶水" }
  ],

  "Content": {
    "buyWaterNormal": 70,
    "buyWaterSmallTalk": 30
  },

  "End up": "REDO",
  "Next Node": null
}
```

### 8.2 Event 欄位

```text
ID           Event ID
Name         創作者可讀的顯示名稱
Trigger      觸發來源，例如 Action:buyWater 或 Auto
Priority     優先權，數字越小越優先
Weight       同 Priority 事件中的抽選權重
Once         是否全遊戲只觸發一次
Conditions   觸發條件
Effects      對 Stats、Memories、音效等造成的效果
Content      要呼叫的 Ren'Py label，可為 null
End up       節點結束行為：REDO / GOTO / EXIT
Next Node    End up 為 GOTO 時的目標節點
```

### 8.3 Priority

Priority 目前設計為 `0` 到 `5`。

數字越小優先權越高。

建議保留：

```text
0   系統保留
1   特殊事件保留
2-5 創作者一般使用
```

### 8.4 Weight

當多個 Event 同時符合：

```text
Trigger 相同
Conditions 通過
Priority 相同且為最高優先層
```

系統會依照 Weight 抽選一個 Event。

抽選機率為：

```text
自身 Weight / 同層候選 Event 的 Weight 總和
```

### 8.5 Once

`Once: true` 代表此 Event 全遊戲只會觸發一次。

實作上等同於系統在預設 `memory` 記憶庫註冊一筆記憶，例如：

```text
once:<event_id>
```

之後選取 Event 時，若該 once 記憶已存在，該 Event 不再成為候選事件。

---

## 9. Conditions

Conditions 使用陣列格式，方便擴充不同條件類型。

### 9.1 Stat 條件

```json
{ "type": "stat", "id": "Money", "op": ">=", "value": 50 }
```

可用比較運算：

```text
>
>=
<
<=
==
!=
```

### 9.2 Memory 條件

```json
{ "type": "memory", "bank": "memory", "id": "得到一瓶水", "op": "has" }
```

```json
{ "type": "memory", "bank": "daily", "id": "今天已打工", "op": "not_has" }
```

可用記憶判斷：

```text
has
not_has
```

### 9.3 無條件 Event

若 `Conditions` 為空陣列，代表此 Event 無條件成立。

```json
"Conditions": []
```

每個玩家可選 Trigger 建議至少準備一個無條件 Event 作為緩衝。

---

## 10. Effects

Effects 同樣使用陣列格式。

### 10.1 Stat Effect

```json
{ "type": "stat", "id": "Money", "op": "-", "value": 50 }
```

建議支援：

```text
set
+
-
*
/
```

範例：

```json
{ "type": "stat", "id": "Energy", "op": "set", "value": 100 }
```

### 10.2 Memory Effect

```json
{ "type": "memory", "bank": "daily", "op": "add", "id": "今天已打工" }
```

```json
{ "type": "memory", "bank": "memory", "op": "remove", "id": "得到一瓶水" }
```

清空整個記憶庫：

```json
{ "type": "memory", "bank": "daily", "op": "clear" }
```

### 10.3 Scene Effect

Scene Effect 可作為未來擴充，用來處理 BGM、SE、環境音、濾鏡等跨節點效果。

範例：

```json
{ "type": "bgm", "op": "play", "id": "rainLoop", "persistent": true }
```

```json
{ "type": "se", "op": "play", "id": "doorKnock", "persistent": false }
```

`persistent` 的概念：

```text
true   進入子節點後仍持續
false  只作用於目前節點生命週期
```

此部分可先預留，不一定在第一版完整實作。

---

## 11. Content

Content 可有三種形式。

### 11.1 不呼叫 Content

```json
"Content": null
```

### 11.2 固定呼叫單一 label

```json
"Content": "buyWaterNormal"
```

### 11.3 權重抽選 label

```json
"Content": {
  "buyWaterNormal": 70,
  "buyWaterSmallTalk": 30
}
```

權重不需要加總為 100。系統會用各自權重除以總權重計算機率。

若不同演出版本的 Effects 相同，只是文字或演出不同，適合放在同一個 Event 的 Content 權重中。

若不同結果會造成不同 Effects 或 Conditions，應拆成不同 Event。

---

## 12. Next Node

`Next Node` 只在 `End up` 為 `GOTO` 時使用。

### 12.1 不前往其他節點

```json
"Next Node": null
```

### 12.2 固定前往單一節點

```json
"Next Node": "streetNormal"
```

### 12.3 權重抽選節點

```json
"Next Node": {
  "streetNormal": 80,
  "streetRain": 20
}
```

---

## 13. CONTENT/<label_name>.rpy

Content label 由創作者自行撰寫。

範例：

```renpy
label buyWaterNormal:
    "你買了一瓶水。"
    return
```

規則：

```text
Content label 必須 return
Content label 原則上只負責演出
不要在 Content label 中直接跳轉 Scene Node
不要在 Content label 中繞過 Runner 改變主要流程
```

若確實需要特殊演出邏輯，可以在實作階段由框架提供額外 API。

---

## 14. Scene Node Runner

Runner 是框架的核心執行器。

定案流程如下：

```text
Enter Scene Node（未傳入 ID 時使用 SceneProject.json 的 Root Node）
1. load Node.json
2. show Background
3. show Scene Screen
4. run Auto Events until none matched
5. call Options.json Renderer 或自訂 SCENEOPTION.rpy and wait Trigger
6. select one Event from EVENTPOOL
7. choose weighted Content if needed
8. choose weighted Next Node if needed
9. apply Effects
10. call Content label if Content is not null
11. resolve End up
```

Effects 先於 Content 套用。
因此 Content label 中看到的是事件發生後的 State。

---

## 15. Event 選擇流程

當 Runner 收到 Trigger 後，依序執行：

```text
1. 從目前 Scene Node 的 EVENTPOOL 取得所有 Event
2. 篩選 Trigger 相同的 Event
3. 排除 Once 已觸發的 Event
4. 檢查 Conditions
5. 找出 Priority 數字最小的一層
6. 在該層中依 Weight 抽出一個 Event
```

若沒有符合的 Event：

```text
開發階段：顯示明確錯誤
正式階段：可選擇回到目前節點或呼叫預設 fallback
```

內容設計上，每個玩家選項應準備至少一個無條件 Event，避免沒有事件可選。

---

## 16. Auto Event

Auto Event 使用保留 Trigger：

```text
Auto
```

進入 Scene Node 時，Runner 會先執行 Auto Event 檢查。

Auto Event 會重複執行，直到沒有符合條件的 Auto Event 為止。

典型 Auto Event：

```json
{
  "ID": "autoMorningGreeting",
  "Trigger": "Auto",
  "Priority": 2,
  "Weight": 1,
  "Once": true,
  "Conditions": [
    { "type": "stat", "id": "DayTime", "op": "==", "value": 0 }
  ],
  "Effects": [],
  "Content": "morningGreeting",
  "End up": "REDO",
  "Next Node": null
}
```

Auto Event 通常使用 `REDO`，讓節點在 State 改變後重新檢查 Auto 與選項。

若 Auto Event 使用 `GOTO` 或 `EXIT`，Runner 會立即處理節點堆疊，不再繼續顯示目前節點的選項。

實作時可加入開發期警告，例如同一輪 Auto 執行次數異常過多時提醒可能有條件循環。但目前不將此視為內容格式的一部分。

---

## 17. Scene Stack

雖然抽象上 Scene Node 可以設計成樹狀結構，但實際執行使用 stack。

```text
Root Node
  GOTO child node   -> push child
  EXIT child node   -> pop child, return parent
  REDO current node -> stack 不變，重跑目前節點
```

### 17.1 REDO

`REDO` 代表重新執行目前 Scene Node 的完整流程。

包含：

```text
load Node
show Background
show Screen
run Auto Events
show Scene Options
```

### 17.2 GOTO

`GOTO` 代表生成子節點。

Runner 將目標 Scene Node push 到 stack 頂端，並開始執行該子節點。

### 17.3 EXIT

`EXIT` 代表離開目前 Scene Node，回到父節點。

Runner 將目前節點 pop 出 stack。
若父節點存在，父節點會重新進入自己的流程。
若目前節點已是 root，則結束整個 Scene Node Runner，回到 Ren'Py 外層流程。

---

## 18. 暫緩項目

以下項目目前先不作為核心格式定案，但實作時可逐步加入：

```text
JSON / label / node 驗證器
瀏覽器渲染的 GUI 編輯器
完整 Scene Effect 生命週期管理
更複雜的條件群組，例如 AND / OR / NOT 巢狀邏輯
更完整的時間系統設定
```

其中驗證器未來可以檢查：

```text
Event 指向的 Content label 是否存在
Next Node 是否存在
Stat 名稱是否存在
Memory Bank 是否存在
Screen 名稱是否存在
Priority / Weight 是否合法
JSON 格式是否正確
```

---

## 19. 目前定案摘要

目前架構可概括為：

```text
全遊戲唯一 State
可由創作者定義的 Memory Banks
多個 Scene Node
每個 Scene Node 擁有自己的 Event Pool
玩家 Option 由 Options.json 或自訂 RPY 呈現，且只產生 Trigger
Event 決定 Effects / Content / End up
Content 是 Ren'Py label
Scene Screen 是可重用 UI 外殼
GOTO / EXIT / REDO 由 Scene Stack 管理
```

這代表核心資料與演出責任分離：

```text
JSON 負責規則、State 與預設選項
Rpy 負責自訂選項、畫面與演出
Runner 負責流程控制
State Manager 負責資料一致性
```
